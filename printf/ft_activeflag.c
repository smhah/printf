#include "printf.h"
#include "libft/libft.h"

void ft_activeint(char *indices)
{
    int i;

    i = 0;
    while(indices[i])
    {
        if (indice[i] == 9)
            i++;
        if(indice[i] == 0)
            
    }
}